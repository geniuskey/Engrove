---
description: 날짜 중심 주요 일정과 Jira 스타일 실행 작업을 함께 사용하는 방법을 설명합니다.
title: 주요 일정과 작업 관리
---

# 주요 일정과 작업 관리

Engrove는 프로젝트의 시간 구조와 실행 작업을 구분합니다. 이 구분은 일정의 의미를 분명하게 하고,
사람이 직접 진행률 숫자를 관리하는 문제를 줄입니다.

## 주요 일정은 날짜 중심입니다

주요 일정은 시작일·종료일을 가진 작업 묶음이 아니라 **그 날짜 자체가 중요한 프로젝트 이벤트**입니다.

예를 들면 다음과 같습니다.

- 설계 동결
- 시제품 입고
- 시험 완료
- 고객 리뷰
- 양산 승인

이 이벤트들은 대개 순서대로 이어지므로 시작일은 이전 주요 일정과 중복되는 경우가 많습니다.
Engrove는 목표 날짜 하나를 중심으로 한 타임라인을 제공하고, 상태·설명·담당자와 관련 작업을
연결합니다.

진행률을 직접 입력하지 않습니다. 대신 연결된 작업의 완료 수를 보여 주므로, 일정 상태와 실제 실행
데이터가 어긋나는 문제를 줄입니다.

![개념 승인, 설계 동결, 시제품 검증과 양산 릴리스를 한 축에 표시한 주요 일정](/screenshots/key-date-timeline.png)

_예제에서는 `설계 동결`에 작업 하나가 연결되어 `0/1`로 표시됩니다. 이 수치는 사용자가 입력한
퍼센트가 아니라 연결 작업의 실제 완료 상태에서 계산됩니다._

### 주요 일정 작성 예제

| 항목      | 필수 여부 | 예제                                          |
| --------- | --------- | --------------------------------------------- |
| 이름      | 필수      | `Prototype validation`                        |
| 목표일    | 필수      | `2026-09-10`                                  |
| 상태      | 필수      | 위험                                          |
| 설명      | 선택      | 공급사 툴링 전에 중요 시험 이슈를 종결합니다. |
| 연결 작업 | 선택      | 시험 곡선 검토, 편차 승인                     |

시작일과 수동 진행률은 입력하지 않습니다. 일정 사이 기간은 앞선 목표일과 다음 목표일 사이로 읽고,
실행 상황은 연결 작업에서 가져옵니다.

## 작업은 실행 단위입니다

작업은 Jira에서 익숙한 흐름을 따르되 엔지니어링 근거를 일급 정보로 다룹니다.

- 프로젝트별 사용자 정의 상태와 허용 전환
- 칸반, 밀도 높은 목록, 달력
- 담당자, 우선순위, 마감일, 라벨과 저장 필터
- 하위 작업과 파생 완료 맥락
- 차단·선행·관련 작업 관계
- 원래 추정 시간, 남은 시간과 작성자별 작업 기록
- 댓글, 멘션, 구독, 첨부 파일과 외부 URL
- 레코드·측정·평가·파일·데이터셋에 대한 정확한 근거 연결

카드를 선택하면 상세 패널에서 바로 수정할 수 있습니다. 카드 자체에는 상태 콤보박스나 반복되는
메뉴를 두지 않아 보드를 빠르게 읽을 수 있고, 이동은 드래그, 키보드 또는 우클릭 메뉴로 수행합니다.

![해야 할 일, 진행 중, 검토와 완료 열로 구성된 작업 보드](/screenshots/task-board.png)

_카드에는 식별자, 제목, 담당자와 우선순위처럼 훑어보기에 필요한 정보만 남깁니다._

![선택한 작업을 같은 화면에서 수정하는 상세 패널](/screenshots/task-detail.png)

_상세 패널에서 제목·설명·상태·우선순위·담당자와 선택 정보를 수정합니다. 필수와 선택 표시는
작성자가 저장 조건을 추측하지 않게 합니다._

### 작업 작성 예제

`Review Demo Run 001 force curve` 작업이라면 제목과 상태만으로 생성한 뒤, 실행에 필요한 맥락을
점진적으로 더할 수 있습니다.

- 필수: 제목, 상태
- 선택: 설명, 우선순위, 담당자, 마감일, 라벨, 추정 시간
- 근거: 시험 레코드, 평가 결과, 원본 파일 또는 외부 URL
- 관계: 상위·하위 작업, 선행·차단·관련 작업

## 작업과 데이터의 연결

실패한 사양 평가에서 후속 작업을 만들면 다음 정보가 한 트랜잭션으로 연결됩니다.

1. 평가 대상 레코드와 필드
2. 측정값과 단위
3. 사용된 사양 리비전과 판정
4. 생성된 작업과 감사 이벤트

따라서 작업 제목만 보고 원인을 추정하지 않아도 됩니다. 작업의 근거에서 정확한 데이터로 이동하고,
레코드에서는 아직 열려 있는 후속 작업을 확인할 수 있습니다.

## 검토와 개인 작업함

모든 의견을 작업으로 만들 필요는 없습니다. 레코드 검토는 검토자 지정, 대화, 승인 또는 변경 요청과
해결 이력을 제공합니다. 개인 작업함은 여러 프로젝트에 흩어진 담당 작업과 요청받은 검토를 한곳에
모아 줍니다.

## 흐름과 자동화

프로젝트 리드는 작업 흐름 인사이트에서 현재 WIP, 오래된 작업, 처리량, 중앙값·P85 사이클 타임과
누적 흐름을 확인할 수 있습니다. 자동화 규칙은 상태 변경 같은 이벤트에 조건을 적용해 작업 갱신,
알림 등의 동작을 트랜잭션 안에서 수행하고 실행 이력을 남깁니다.

세부 계약은 [작업 협업](/product/task-collaboration), [작업 워크플로](/product/task-workflow),
[작업 자동화](/product/task-automation)를 참고하세요.
